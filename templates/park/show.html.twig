{% extends 'base.html.twig' %}

{% block title %}Park{% endblock %}

{% block body %}
    <h1>Park</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ park.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ park.name }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ park.country }}</td>
            </tr>
            <tr>
                <th>Année d'ouverture</th>
                <td>{{ park.openingYear }}</td>
            </tr>
        </tbody>
    </table>

    <div class="d-flex gap-2"> 
        <a href="{{ path('app_park_edit', {'id': park.id}) }}" class="btn btn-primary mb-3">Modifier</a>

        {{ include('park/_delete_form.html.twig') }}
    </div>

    <a href="{{ path('app_park_index') }}" class="btn btn-secondary mb-3">Retour</a>
{% endblock %}
