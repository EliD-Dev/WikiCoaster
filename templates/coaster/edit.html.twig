{% extends 'base.html.twig' %}

{% block title %}Modifier le Coaster{% endblock %}

{% block body %}
    <h1>Modifier le Coaster</h1>

    {{ form_start(coasterForm) }}
        {{ form_widget(coasterForm) }}
        <button type="submit" class="btn btn-primary mb-3">Enregistrer les modifications</button>
        <a href="{{ path('app_coaster_index') }}" class="btn btn-secondary mb-3">Retour</a>
    {{ form_end(coasterForm) }}
{% endblock %}