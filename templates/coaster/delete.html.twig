{% extends 'base.html.twig' %}

{% block title %}Supprimer {{ coaster.name }}{% endblock %}

{% block body %}
    <h1>Supprimer {{ coaster.name }}</h1>

    <p>Êtes-vous sûr de vouloir supprimer ce coaster ? Cette action est irréversible.</p>

    <form method="post">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ coaster.id) }}">
        <button type="submit" class="btn btn-danger">Supprimer</button>
        <a href="{{ path('app_coaster_index') }}" class="btn btn-secondary">Annuler</a>
    </form>
{% endblock %}
